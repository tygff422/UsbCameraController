[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "usb-camera-adapter"
version = "0.1.0"
requires-python = "~=3.11.0"
dependencies = [
    "loguru>=0.7.3",
    "numpy>=2.4.6",
    "opencv-python>=5.0.0.93",
    "adapter-core",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.hatch.build.targets.wheel]
packages = ["src/usb_camera_adapter", "src/usb_camera_controller", "src/interfaces"]

[tool.uv.sources]
adapter-core = { workspace = true }
